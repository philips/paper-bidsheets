Only output the CSV don't output anything else in the response like "This is the CSV:". The output format will be bidder number, name, and dollar amount extracted from the handwritten text in the columns. Only output CSV.
