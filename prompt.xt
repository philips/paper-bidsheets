The image contains a scan of a form with handwritten data in columns for bidder number, name, and dollar amount. It also contains printed information like package number. Extract the handwritten data and the printed package number into a CSV with package, bidder number, name, and dollar amount. Only output the CSV don't output anything else in the response like "This is the CSV:".
